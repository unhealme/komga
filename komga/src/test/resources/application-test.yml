application.version: TESTING

komga:
  database:
    url: jdbc:postgresql://localhost:5432/komga?user=komga&password=komga

spring:
  flyway:
    enabled: true

logging:
  level:
    org.gotson.komga: DEBUG
#    org.jooq: DEBUG
#    org.jooq.tools.LoggerListener: DEBUG
